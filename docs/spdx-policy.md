# SPDX Policy

The filtered history retains its original licensing terms. New standalone
source files created at and after the verified bootstrap commit will use
`SPDX-License-Identifier: Apache-2.0` where comment syntax permits. Generated
JSON, Markdown, fixtures, and license texts rely on the repository `LICENSE` and
`NOTICE` files instead of inline headers.
